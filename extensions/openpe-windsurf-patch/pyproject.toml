[build-system]
requires = ["setuptools>=64", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "openpe-windsurf-patch"
version = "0.2.0.dev0"
description = "Experimental profile-gated IDE bundle patch installer for openPE"
readme = "README.md"
license = {text = "MIT"}
requires-python = ">=3.8"
authors = [{name = "openPE contributors"}]
keywords = ["openpe", "devin", "windsurf", "patch", "installer", "experimental"]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Environment :: Console",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: MIT License",
    "Operating System :: MacOS :: MacOS X",
    "Operating System :: Microsoft :: Windows",
    "Operating System :: POSIX :: Linux",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3 :: Only",
    "Programming Language :: Python :: 3.8",
    "Programming Language :: Python :: 3.9",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Topic :: Software Development :: Build Tools",
]
# Intentionally no runtime dependencies: stdlib only. Keeps the install
# footprint small and removes supply-chain risk for a tool that modifies
# user binaries.
dependencies = []

[project.urls]
Homepage = "https://github.com/AoManoh/openpe"
Repository = "https://github.com/AoManoh/openpe"
Issues = "https://github.com/AoManoh/openpe/issues"

[project.scripts]
openpe-ide-patch = "installer.cli:main"
openpe-windsurf-patch = "installer.compat_windsurf:main"

[tool.setuptools.packages.find]
where = ["."]
include = ["installer*"]
exclude = ["tests*", "inject*", "docs*", "scripts*"]

[tool.setuptools.data-files]
"share/openpe-windsurf-patch/inject/dist" = ["inject/dist/inject.js"]
