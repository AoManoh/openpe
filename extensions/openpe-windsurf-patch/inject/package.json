{
  "name": "openpe-ide-inject",
  "version": "0.1.0-dev",
  "private": true,
  "description": "openPE profile-gated IDE bundle injection payload (esbuild → dist/inject.js IIFE)",
  "license": "MIT",
  "type": "module",
  "engines": {
    "node": ">=22.18.0"
  },
  "scripts": {
    "build": "node esbuild.config.mjs",
    "check": "node ./node_modules/typescript/bin/tsc --noEmit",
    "test": "node --test tests/*.test.mjs",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "esbuild-wasm": "0.24.2",
    "typescript": "5.9.3"
  }
}
