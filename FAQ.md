# openPE 常见问题（FAQ）

面向使用者的行为解释与排查。首次安装见 [README](README.md)，参数效果和使用案例见 [CONFIG.md](CONFIG.md)，客户端接入细节见 [CLIENTS.md](CLIENTS.md)。

---

## 〇、安装与环境

### Q0. `go install` 成功了，但 `openpe：未找到命令`？

**现象示例**（真实案例，2026-08 新机器首装）：

```
❯ go install ./cmd/openpe
go: downloading modernc.org/sqlite v1.52.0
...（正常结束，无报错）

❯ openpe -h
openpe：未找到命令

❯ which go
/usr/local/go/bin/go        # 该目录里只有 go、gofmt，没有 openpe
```

**这不是构建失败**——`go install` 正常结束就说明 binary 已产出。它写入的是 `$GOBIN`（未设置时为 `$GOPATH/bin`，通常 `~/go/bin`），**不是** Go 工具链所在的 `/usr/local/go/bin`；找不到命令只是因为产物目录不在 `PATH`。`source ~/.bashrc` 救不回来——你的 rc 文件里本来就没有这条 PATH。

排查与修复：

```bash
# 1. 确认 binary 确实在产物目录（应能看到 openpe）
ls "$(go env GOPATH)/bin"
# 若这里没有，再查 go env GOBIN——GOBIN 被设置过时产物在那个目录

# 2. 把产物目录永久加入 PATH（zsh 用户改 ~/.zshrc）
echo 'export PATH="$PATH:$(go env GOPATH)/bin"' >> ~/.bashrc
source ~/.bashrc

# 3. 验证
openpe -h
```

两个相关的「正常现象」不要误判为安装问题：

- `cd ~/.config/openpe` 报「没有那个文件或目录」：该目录是[快速开始第 2 步](README.md#2-配置模型-api)由你手动 `mkdir -p` 创建的配置目录，安装不会创建它。
- `/usr/local/go/bin` 下没有 openpe：那里永远只有 Go 工具链自身（`go`、`gofmt`），`go install` 从不往里写。

### Q0.1. 配置 Anthropic 地址后，为什么返回 `openai-compatible provider HTTP 404`？

**现象**：API key 和模型名看起来都正确，但反馈类似：

```text
openPE 增强失败：openai-compatible provider returned HTTP 404:
{"detail":"Not Found"}
```

如果 `OPENPE_BASE_URL` 指向 Anthropic Messages 兼容端点，通常是因为没有显式选择 Anthropic 协议。

openPE 默认使用 OpenAI-compatible 协议：

```text
POST <base-url>/v1/chat/completions
Authorization: Bearer <key>
```

Anthropic Messages 端点需要：

```text
POST <base-url>/v1/messages
x-api-key: <key>
anthropic-version: 2023-06-01
```

修复方法是在 dotenv 中增加 `OPENPE_PROVIDER=anthropic`：

```dotenv
OPENPE_PROVIDER=anthropic
OPENPE_BASE_URL=https://your-anthropic-compatible-endpoint
OPENPE_API_KEY=replace-with-your-api-key
OPENPE_MODEL=your-model
```

`OPENPE_MAX_TOKENS` 可选。Anthropic 协议要求 `max_tokens`；不设置或设为 `0` 时，openPE 使用 4096。

保存后，hook 的下一次调用会重新读取 dotenv。也可以先用裸 CLI 验证：

```bash
OPENPE_ENV_FILE="$HOME/.config/openpe/.env" \
openpe enhance --prompt "帮我整理这个需求"
```

判断是否修好：

- 错误文案中的 provider 应从 `openai-compatible` 切换为 `anthropic`；
- 请求路径应为 `/v1/messages`；
- 正常返回增强提示词说明协议、地址、密钥和模型名均可用。

如果你的网关明确提供 `/v1/chat/completions`，则继续使用默认 `OPENPE_PROVIDER=openai`，不要因为模型名称里包含 Claude 就自动切到 Anthropic。协议由 API 端点决定，不由模型名称决定。

---

### Q0.2. 怎么查版本、怎么升级？`openpe update` 报错怎么办？

查版本：`openpe --version`（server 用 `openpe-server --version`）。`go install …@vX.Y.Z` 装的显示发布 tag；克隆仓库本地构建显示伪版本（脏工作区带 `+dirty`）；显示 `devel` 说明构建没有版本信息（如 `go run`）。

升级：`openpe update`——查询 Go module proxy 后直接重装两个二进制；`openpe update --check` 只查不装。它遵循你的 `GOPROXY`（国内镜像用户走自己的镜像），远端版本不高于当前时不会安装（永不降级）。

报错处理：查询失败（网络不可达、proxy 无法访问）或安装失败时，命令会打印具体错误和等价的手动命令（`go install github.com/AoManoh/openpe/cmd/openpe@latest` 等），按提示手动执行即可。升级后 `openpe --version` 验证；openpe-server 若在运行需重启。有新版本时 `pe` 反馈里也会出现一次限频提醒，`OPENPE_UPDATE_NOTICE=false` 可关闭（见 [CONFIG.md](CONFIG.md#版本与更新)）。

---

## 一、会话历史与时效窗口

**现象示例**（真实案例：同一会话里连续两次相同的 `pe`，结果不同）：

```
❭ pe：继续 Phase 3（隐患运营/审核中心/任务中心 + 筛选紧凑化）

✱ Prompt blocked: openPE：最近会话已超出时效窗口，本次未带前文上下文。 openPE 已拦截原始消息并发送 OSC52 剪贴板序列；若终端支持，请粘贴后编辑发送。

❭ pe：继续 Phase 3（隐患运营/审核中心/任务中心 + 筛选紧凑化）

✱ Prompt blocked: openPE：已带入 12 条会话历史作为上下文。 openPE 已拦截原始消息并发送 OSC52 剪贴板序列；若终端支持，请粘贴后编辑发送。
```

- 第一次：会话空闲已超时效窗口（该案例约 8 小时）→ **不带历史**，增强结果是通用版本（无法解析"Phase 3"指代的前文）。
- 第二次：第一次的提交让 Devin 刷新了会话活跃时间戳 → 落回窗口内 → **带入最近 12 条历史**，增强结果包含会话历史（能带出前文的 commit、分支、计划文件等）。
- 两次都是**成功交付**（剪贴板里是增强结果），差别只在带没带历史。逐条解释见下面 Q1–Q3；想避免第一次落空见 Q4。

### Q1. 为什么提示「最近会话已超出时效窗口，本次未带前文上下文」？

这是**按设计的非静默披露**，不是错误。Devin 的 hook 载荷只有 prompt 一个字段（当前这条 prompt 此刻还没写进会话库），openPE 定位当前会话分两级：

1. **精确识别（Linux，2026-07 起默认）**：hook 进程沿进程链上溯，找到所属 devin 进程持有的 `session_locks/<session-id>.lock`，得到**确定的当前会话**——直查其历史，**不受时效窗口限制**（同一会话隔天 resume 也是同一段对话）。同目录并行多会话（多角色工作流）安全。
2. **回退启发式（非 Linux / 进程链探测失败）**：按「**工作目录 + 最近活跃**」猜测，此时才有两道闸门——
   - **时效窗**：定位到的会话最近活跃须在 `OPENPE_DEVIN_HISTORY_RECENCY`（默认 **6h**）内，超窗判 stale → **丢弃全部历史**、显式披露（即本提示）；
   - **多会话歧义**：同目录窗口内有 ≥2 个会话 → 判 ambiguous → **不带历史**并披露「本目录存在多个活跃会话、无法确定当前会话」。**宁可不带、也不带错**——2026-07-02 曾发生启发式把同目录另一会话（转正述职 PPT）的历史注入 e2e 测试 prompt 的串扰事故，精确识别与该闸门即为此修复。

> Codex / Claude 的定位方式不同且自带真身份（Codex 用 `~/.codex/history.jsonl` 反查 session id；Claude 用 hook 提供的 `transcript_path`），无此问题。时效窗口这一档是 Devin 回退路径特有的。

### Q2. 历史是「按时间戳收录全量历史、超时就截断」吗？

**不是**——这里有两个**互相独立**的机制，容易混淆：

1. **时效闸门（会话级、全有或全无）**：只看**会话**的「最近活跃时间戳」。超过窗口 → 这次**一条历史都不带**；在窗口内 → 进入下一步。它**不是**按每条消息的时间戳做「新的留、旧的截断」。
2. **体量上限（消息级）**：真正带历史时，只取**最近的若干条**——`OPENPE_DEVIN_HISTORY_MAX_MESSAGES`（默认 **12** 条）/ `OPENPE_DEVIN_HISTORY_MAX_CHARS`（默认 **12000** 字）。这是按**条数/字符预算**裁，也不是按时间戳裁。
3. **「带多少历史」只有这一个旋钮**：曾短暂存在的 `OPENPE_HISTORY_RATIO` 梯度档（full/high/med/low）**已移除**（2026-07 Part 4）——按比例叠在「已截好的 12 条」上会造成 `full`＝整个会话的误读，且同一档位的实际轮数随采集条数漂移，成本/质量映射不稳定。现在设该变量会被静默忽略；调历史量就改 `OPENPE_*_HISTORY_MAX_MESSAGES`（绝对轮数、就近保留）。

所以即使一个会话有几千条消息，openPE **也只发最近约 12 条**，不是全量历史。

**12 条 / 12000 字是怎么定的**：这是**工程默认值，不是评测调优出来的最优解**——最早为 Codex 历史采集设定的保守起点（约 6 轮对话、~3k token 量级，兼顾成本与"够解析常见指代"），Claude/Devin 采集器为保持行为一致而沿用。上限取多少更优目前**没有已发布的评测结论**；可按需在 dotenv 里调（`OPENPE_*_MAX_MESSAGES/_MAX_CHARS`）。

### Q3. 为什么「第一次没带历史、第二次带了」？两次增强的 prompt 为什么不同？

> 2026-07 更新：本题描述的是**回退启发式路径**的现象。Linux 精确识别（Q1 第 1 级）下该场景已消失——resume 昨天的会话后第一条 `pe` 就能带上历史（识别成功不受时效窗限制）。以下机制仍适用于非 Linux / 探测失败的回退路径。

先纠正两个说法：

- **第一次不是「失败」**：它成功产出了一条合法的增强 prompt，只是**没有历史**（因此比较通用/欠具体）。失败是指 provider 报错或空输出，那会是另一种提示。
- **openPE 没有「激活」任何上下文**：openPE 对会话库是**只读**的，从不写入、也不会去「激活」窗口。

真正发生的是——**会话的「最近活跃时间戳」在两次调用之间越过了时效窗口**：

1. 早上你**刚恢复**一个昨晚就在用的长会话。第一条 `pe` 触发时，这条 prompt 还没被提交，会话的最近活跃时间戳仍停在**昨晚**（超出时效窗口）→ 定位为 **stale** → 不带历史 → 通用增强（就是你看到的那条）。
2. 之后你与该会话有了新的交互（提交/继续对话），**Devin 把该会话的最近活跃时间戳更新到「刚刚」**。
3. 第二条 `pe` 触发时，**同一个会话**已在窗口内（fresh）→ 命中历史 → 取它最近的约 12 条（含 Phase 2/3 讨论）→ 上下文丰富的增强。

所以你的直觉「第一条让第二条带上了上下文」——**结果上对，机制上要修正**：让会话变「新鲜」的是**你自己的活跃**被 Devin 记录，不是 openPE；而且第一条并没有「失败」。两次 prompt 不同，正是因为第二次真的带了那段 Phase 2/3 历史、第一次没带。

### Q4. 我想让「继续 xxx」这类延续 prompt 总能带上历史，怎么办？

- **Linux 下升级到含精确识别的版本即可**（2026-07 起）：识别成功时延续 prompt 总能命中本会话历史，无需调窗口。以下手段仅在回退路径下才需要：
- 同日常复用：把窗口调大，写进 hook dotenv（如 `~/.config/openpe/.env`）：`OPENPE_DEVIN_HISTORY_RECENCY=12h`。
- 或：先在会话里有一次新活跃，再敲 `pe`（现象示例里第二次带入历史就是这么来的）。
- **取舍**：窗口调得越大，越可能把「其实已经废弃的同目录旧会话」的陈旧上下文也带进来。默认 **6h** 是「延续性 vs 防陈旧泄漏」的折中（2026-07 由 2h 上调：2h 下"隔夜/隔半天恢复"的第一条延续 prompt 总是落空，正是现象示例那种情况）。

> 延伸：延续/指代型 prompt（「继续 Phase 3」「把它也改了」）在**没有历史**时只能被增强成通用版本——这与本项目对「指代解析依赖历史」的评测结论一致（见 `docs/development/2026-07-01-part3-refbind-ab-findings.md`，本地）。

### Q5. 会话被压缩（compaction/续聊摘要）之后，第一条 `pe` 有没有历史？

**有——压缩摘要会作为历史带入**（2026-07-03 起），披露会写「已带入 N 条会话历史（含前文压缩摘要）」。

机制：Devin 压缩会话时，会把主链上的前文替换成一条 **system 角色**的摘要节点（你看到的"You are continuing work from a previous conversation thread…"就是它，携带上一段对话的完整总结）。openPE 的历史采集本来只保留 user/assistant 轮次、过滤 system 噪声——2026-07-03 的事故正是这个过滤器把摘要也丢了：压缩后紧接着的 `pe` 报「会话历史为空」（其实库里有近万个节点），增强退化成零上下文通用版（"汇报一下现在的状态"被膨胀成一张通用仓库体检清单）。修复后，带 `summarized_from` 标记的摘要节点被识别并**映射为 assistant 轮次**带入。

两个边界，防止误解：

- 摘要**占用同一份 12 条 / 12000 字预算**（超长摘要会被截断，新对话轮次积累多了之后摘要会先被挤出——此时披露也不再声称"含摘要"）。
- 若压缩后你已经聊了很多轮，摘要被挤出属正常裁剪，不是回归。

### Q6. 「会话历史为空」还会在什么情况下出现？

修复压缩场景后，这个披露只剩真正的空会话：定位到的会话确实还没有任何可复用的 user/assistant 轮次（例如全新会话的第一条消息就是 `pe`，或此前轮次都是 system/tool 噪声、被取消的空回复）。看到它时增强仍会成功产出，只是不含前文——通用化是预期行为，不是失败。

### Q6.1. 恢复 Devin 会话后，为什么第一次提示「读取历史上下文失败」？

这是旧版历史读取实现的性能问题，不是预期特性。问题在包含大量历史节点的长会话上最明显：旧实现为了取最近 12 条消息，会先把该 session 的全部 `message_nodes` 读入内存。数据库文件页尚未进入系统缓存时，第一次读取可能超过固定 5 秒上限；第二次读取因为数据已经进入缓存而成功。

修复后，openPE 从 `main_chain_id` 开始，使用 `(session_id, node_id)` 索引沿父节点向前读取；达到 12 条 / 12000 字预算就停止。被过滤的 system/tool 节点仍需沿主链穿越，但一次最多检查 4096 个物理节点；触发上限时会明确提示更早内容未读取，不会继续无界扫描。对数据库启动阶段的临时锁或查询超时，还会重新打开只读连接并有界重试一次。

实际复现数据：3.7 GB 的 `sessions.db`、约 3.4 万个会话节点，旧版冷读取在 5.01 秒时报 `context deadline exceeded`；修复后同一会话冷读取 20 次全部成功，单次最慢约 48ms。另一个约 2.2 万节点会话最慢约 205ms。

如果升级后仍看到该提示，请保留括号中的完整错误，例如 `database is locked`、`no such table` 或 `context deadline exceeded`。这类文案只用于真实数据库错误；“未找到 session”“历史为空”“超出时效窗口”都有独立提示。调整 `OPENPE_DEVIN_HISTORY_RECENCY` 或模型的 `OPENPE_TIMEOUT` 不能修复数据库读取错误。

---

## 二、交付与「Prompt blocked」

### Q7. 为什么显示「Prompt blocked / openPE 已拦截原始消息」？剪贴板里是原文还是增强结果？

这是**非注入（review）模式**的正常成功交付（默认 `OPENPE_HOOK_INJECT=false`，各客户端可用 `OPENPE_<CLIENT>_INJECT` 覆盖）：

- openPE **拦下你那行原始 `pe：…`**（不让 agent 去执行这段字面量），然后把**增强后的 prompt** 放进剪贴板；
- 「拦截原始消息」= **挡下原始输入**，**不是**「只复制了原文」。剪贴板里是**增强结果**，粘贴（Ctrl/Cmd+V）、按需编辑、再发送即可。

### Q8. 提示发了 OSC52，但粘贴出来还是旧内容？

OSC52 需要**终端支持**才能真正写入系统剪贴板。若你的终端/IDE 子进程不认 OSC52（如无控制 TTY 的 IDE 子进程、部分远程终端），剪贴板不会更新，这时粘贴到的是**旧内容**。判断与兜底：

- 粘贴后若发现不是本次增强结果 → 用 `openpe <client> hook last --prompt` 打印，或直接打开缓存文件 `last-prompt.txt`（路径见 stderr / 「调用方式」）。
- 若 stderr 明确出现「剪贴板未更新，请勿直接粘贴旧内容」→ **不要按 Ctrl+V**，走上面的兜底。

### Q9. 我想让它直接注入而不是「拦截 + 剪贴板」，可以吗？

可以（在支持注入的宿主：Devin / Codex / Claude）。设 `OPENPE_HOOK_INJECT=true`（全局）或 `OPENPE_<CLIENT>_INJECT=true`（单客户端）。默认关闭是 openPE 的「绝不自动应用、让你先审阅」哲学；Windsurf 无法接收 hook 注入，该开关在其上是有记录的空操作。

### Q10. 拦截后我立刻重发了同一条 `pe`（比如粘贴失败想重试），会发生什么？

**同一会话 5 秒内：重放上次的拦截，不重新增强。**你看到的消息与首次**完全一致**（历史披露、告警原样复现），增强结果会**再次复制到剪贴板**——粘贴失败时重发一次即可兜底。超过 5 秒重发则正常重新增强。

为什么重放不标注"这是重复"：Devin 会把已加载的 openPE hook 全部跑一遍（见 Q14），openPE 无法区分「同一次提交的兄弟 hook」和「你快速重发」，于是重放选择对两种情况都成立的行为——内容与首次一致、不猜测你的动机、绝不放行原文。想确认实际发生了什么：`openpe devin hook last --prompt` 查看缓存的增强产物。

两条边界：注入模式下的重复触发会静默跳过，避免同一条消息注入两次；增强失败或超时后，5 秒内重发同一文本会重放同一失败拦截，原始 `pe` 不会被放行。

### Q10.1. `pe+三问` 提示「规范未找到」，规范文档怎么建？

`pe+<名字>` 会读取 `~/.config/openpe/specs/<名字>.md` 并把内容逐字附到增强结果后段。第一次使用前需要自己创建目录和文档：

```bash
mkdir -p ~/.config/openpe/specs
cat > ~/.config/openpe/specs/三问.md <<'EOF'
完成任务过程中持续三问自己：
1. 是否偏离核心目标？
2. 是否过度设计？
3. 是否还有业务暗坑？
EOF
```

要求：UTF-8 纯文本；文件名（不含 `.md`）就是点名用的名字；内容为空或超过 `OPENPE_SPEC_MAX_CHARS`（默认 8000 字符）会直接报错。「未找到」的提示里会列出目录中现有的规范名，方便核对拼写。openPE 故意不做静默降级：点名失败就阻断本次增强，改正后重发即可，不会把没带规范的结果冒充成带了规范的。

---

## 三、增强改写质量

### Q11. 增强结果里出现了上下文里没有的数字，或者口吻变成了"助手在问我"？

这两类劣化有真实案例（2026-07-02）：用户对助手报告回一句「验证通过，继续推进」，增强结果把报告里"需要你决策/我可以起草/请明确你希望哪项"等**助手对用户说的话**原样吸收（人称完全反转），还给上下文中没给出数字的条目**编造精确数值**（且每次编的不一样）。

- **缓解情况**：内置提示词 v7g（2026-07）加入两条护栏并在末尾复检——增强结果必须是「用户对 agent 的祈使指令」（助手的反问/自称要转写或删除）；数字/组成只能逐字取自上下文中同一条目，总数不得分解成编造的分项。探针矩阵（17 样本 × 6 模型端点）内口吻反转清零、编造数字降为 ~12% 残余；**属候选缓解，系统性验证进行中**。
- **残余怎么防**：剩余臆造集中在「给只提了名字的条目补一个括号数字」。默认 review 交付流就是兜底：**粘贴前扫一眼输出里的数字**，不认识的数字就删掉或改回模糊表述。若你用注入模式（`OPENPE_*_INJECT=true`，跳过人工过目），对含数字汇报类的回应建议把关键数值直接写进你的原始 prompt。

### Q12. 我问的是"你觉得该做什么？"，增强结果怎么变成了一张要执行的任务单？

这是 2026-07-02 的**事故 #3**（v7g 上线当日暴露）：征询式问题「你觉得现在应该做什么？继续基准测试还是做其他方面？」被增强成完整执行指令单，并把上下文里"本地 13 个提交**尚未 push**"这个**状态陈述**翻转成了「push 到远程」的**指令**——用户从未做过这个决策，而下游 agent 按指令真的推送了。

- **缓解情况**：v7h（2026-07）限定"回应→指令"转换**仅适用于用户输入本身表达了决策**（认可/批准/继续/明确选择）；输入是问题时增强结果必须仍是**咨询请求**（评估+建议+理由，可枚举上下文中的候选方向），并明确「上下文中的状态是事实不是授权」，FINAL CHECK 增加对 push/部署/删除/发布等未决策动作的复检。探针级验证：v7h 在 sonnet/opus/gpt-5.5 上 7/7 干净（含模型主动写出"不要擅自执行 push，这里只需要评估和建议"），对照 v7g 为 0/3——**候选缓解**，"合法决策不被弱化"等对照场景由系统性验证覆盖后才下最终结论。
- **教训与兜底**：增强结果里出现**你没决定过的动作**（尤其 push/部署/删除这类不可逆操作）就删掉再发；注入模式用户请特别注意这一条。

### Q13. 增强结果太简洁（或太啰嗦）？`OPENPE_PROMPT_STYLE` 是干什么的？

2026-07 起内置提示词默认为 v7i（"最小忠实展开"），同时以 `OPENPE_PROMPT_STYLE` 保留了上一代 v7h 的详尽风格。这个参数回答一个问题：**增强结果写给谁**。

| 取值 | 写给谁 | 效果 | 适合谁 |
|------|--------|------|--------|
| `agent`（默认） | 下游编码 **agent** | 最小忠实展开：一两句话的任务产出紧凑短提示（典型 100–400 字），多部件任务才用结构化展开；不加分节脚手架、不发明架构词汇、不加仪式步骤 | 把增强结果直接粘给 agent 干活的用户。金标准评测（717 对、冻结 opus 裁判）显示对 agent 执行效果显著更优（86.0%），且**技术决策留给能读到你代码的 agent**——增强器本身看不到仓库，替它做架构决定容易过度设计 |
| `human` | 屏幕前的**人** | 详尽报告体展开（目标/步骤/验证脚手架，典型 300–900 字）。当前为 **v2**（2026-07）：保留 v7h 的结构与体量，修掉其金标准确认的缺陷——每节内容必须可追溯到你的请求（不加戏）、一事一说（不复读）、不发明架构词汇/样例名/仪式步骤 | 喜欢在 review 预览里**读**增强结果的用户——把它当"如何把模糊需求系统性展开"的范文学习，或单纯偏好信息更满的任务书。注意：其展开是在**看不到你仓库**的前提下写的，其中的具体技术方案当模板参考，别当结论 |

两种风格的**护栏完全相同**（同语言输出、用户视角、数值保真、问题保持与决策保真），差别只在详略与结构。

**怎么配**：在 `~/.config/openpe/.env`（hook 默认加载的配置文件）加一行：

```dotenv
OPENPE_PROMPT_STYLE=human
```

- 不填时使用 `agent`。填写不支持的值会在启动时返回明确错误并列出可选值，不会自动改用其它风格。
- hook 路径每次 `pe` 都重新读配置，改完**下一条 `pe` 即生效**；长驻的 `openpe-server` 需重启一次。
- 优先级：`OPENPE_SYSTEM_PROMPT_FILE` / `OPENPE_SYSTEM_PROMPT`（完全自定义提示词）> `OPENPE_PROMPT_STYLE` > 内置默认。设置了前者，本参数不生效。

---

## 四、来自上游宿主的边界

### Q14. 哪些行为是宿主客户端决定的，openPE 只能缓解、不能根治？

这些行为源自 Codex / Claude Code / Devin / Windsurf 自身的协议或实现，openPE 在自己这层做了缓解，但无法改变根因（均为 2026-07 实测确认）：

| 宿主行为 | 对你的影响 | openPE 的缓解 |
|---|---|---|
| Devin 把已加载的 hook **全部跑一遍**（先拦截不短路），展示哪个 hook 的消息由它内部决定、不可预期 | 装了 devin + claude 两种格式时，一次 `pe` 触发两个 hook 进程 | 单飞去重：只增强一次，其余 hook 输出与获胜者**逐字一致**——展示哪个都一样（Q10） |
| Devin **不加载 Windsurf 格式** hook（2026.8.18 实测） | 只装 windsurf 格式时，Devin 里 `pe` 没反应 | 装 devin 或 claude 任一格式即可 |
| hook 载荷只有 prompt 文本，**不带会话标识** | openPE 需要自己判断"这是哪个会话" | Linux 沿进程链精确识别；非 Linux 回退启发式（Q1 的时效窗与多会话拒带；此时并行会话在 5 秒内提交同一段文本，可能复用另一会话的增强结果） |
| **被拦截的消息不落库**、新会话要到首条消息真正发出才建档 | 全新会话的第一条 `pe` 报「会话历史为空」是实况 | 如实披露，不猜测（Q6） |
| 压缩（compaction）会把全部前文**替换成一条摘要** | 压缩后 openPE 拿不到原始对话轮次 | 把摘要作为历史带入并在披露注明（Q5） |
| IDE 子进程无 TTY、远程终端不支持 OSC52 | 剪贴板复制可能失败 | 缓存文件兜底 + 强警告（Q8） |
| Windsurf 不接收 hook 注入 | 注入开关在 Windsurf 上无效 | 有记录的空操作（Q9） |

已向 Devin 上游提交的改进请求：hook 载荷携带会话 id（可让"会话识别"从逆向推断变成协议保证）。
