# openPE 常见问题（FAQ）

面向使用者的行为解释与排查。产品级约束见 [README](README.md) 的「注意事项与已知限制」。

---

## 一、会话历史与时效窗口

**现象示例**（真实案例：同一会话里连续两次相同的 `pe`，结果不同）：

```
❭ pe：继续 Phase 3（隐患运营/审核中心/任务中心 + 筛选紧凑化）

✱ Prompt blocked: openPE：最近会话已超出时效窗口，本次未带前文上下文。 openPE 已拦截原始消息并发送 OSC52 剪贴板序列；若终端支持，请粘贴后编辑发送。

❭ pe：继续 Phase 3（隐患运营/审核中心/任务中心 + 筛选紧凑化）

✱ Prompt blocked: openPE：已带入 12 条会话历史作为上下文。 openPE 已拦截原始消息并发送 OSC52 剪贴板序列；若终端支持，请粘贴后编辑发送。
```

- 第一次：会话空闲已超时效窗口（该案例约 8 小时）→ **不带历史**，增强结果是通用版本（无法解析"Phase 3"指代的前文）。
- 第二次：第一次的提交让 Devin 刷新了会话活跃时间戳 → 落回窗口内 → **带入最近 12 条历史**，增强结果满血（能带出前文的 commit、分支、计划文件等）。
- 两次都是**成功交付**（剪贴板里是增强结果），差别只在带没带历史。逐条解释见下面 Q1–Q3；想避免第一次落空见 Q4。

### Q1. 为什么提示「最近会话已超出时效窗口，本次未带前文上下文」？

这是**按设计的非静默披露**，不是错误。Devin 的 hook 载荷只有 prompt 一个字段（当前这条 prompt 此刻还没写进会话库），openPE 定位当前会话分两级：

1. **精确识别（Linux，2026-07 起默认）**：hook 进程沿进程链上溯，找到所属 devin 进程持有的 `session_locks/<session-id>.lock`，得到**确定的当前会话**——直查其历史，**不受时效窗口限制**（同一会话隔天 resume 也是同一段对话）。同目录并行多会话（多角色工作流）安全。
2. **回退启发式（非 Linux / 进程链探测失败）**：按「**工作目录 + 最近活跃**」猜测，此时才有两道闸门——
   - **时效窗**：定位到的会话最近活跃须在 `OPENPE_DEVIN_HISTORY_RECENCY`（默认 **6h**）内，超窗判 stale → **丢弃全部历史**、显式披露（即本提示）；
   - **多会话歧义**：同目录窗口内有 ≥2 个会话 → 判 ambiguous → **不带历史**并披露「本目录存在多个活跃会话、无法确定当前会话」。**宁可不带、也不带错**——2026-07-02 曾发生启发式把同目录另一会话（转正述职 PPT）的历史注入 e2e 测试 prompt 的串扰事故，精确识别与该闸门即为此修复。

> Codex / Claude 的定位方式不同且自带真身份（Codex 用 `~/.codex/history.jsonl` 反查 session id；Claude 用 hook 提供的 `transcript_path`），无此问题。时效窗口这一档是 Devin 回退路径特有的。

### Q2. 历史是「按时间戳收录全量历史、超时就截断」吗？

**不是**——这里有两个**互相独立**的机制，容易混淆：

1. **时效闸门（会话级、全有或全无）**：只看**会话**的「最近活跃时间戳」。超过窗口 → 这次**一条历史都不带**；在窗口内 → 进入下一步。它**不是**按每条消息的时间戳做「新的留、旧的截断」。
2. **体量上限（消息级）**：真正带历史时，只取**最近的若干条**——`OPENPE_DEVIN_HISTORY_MAX_MESSAGES`（默认 **12** 条）/ `OPENPE_DEVIN_HISTORY_MAX_CHARS`（默认 **12000** 字）。这是按**条数/字符预算**裁，也不是按时间戳裁。
3. **「带多少历史」只有这一个旋钮**：曾短暂存在的 `OPENPE_HISTORY_RATIO` 梯度档（full/high/med/low）**已移除**（2026-07 Part 4）——按比例叠在「已截好的 12 条」上会造成 `full`＝整个会话的误读，且同一档位的实际轮数随采集条数漂移，成本/质量映射不稳定。现在设该变量会被静默忽略；调历史量就改 `OPENPE_*_HISTORY_MAX_MESSAGES`（绝对轮数、就近保留）。

所以即使一个会话有几千条消息，openPE **也只发最近约 12 条**，不是全量历史。

**12 条 / 12000 字是怎么定的**：这是**工程默认值，不是评测调优出来的最优解**——最早为 Codex 历史采集设定的保守起点（约 6 轮对话、~3k token 量级，兼顾成本与"够解析常见指代"），Claude/Devin 采集器为保持行为一致而沿用。「上限本身取多少最优」由 Part 4 N-sweep 实验实测（结论见 `docs/development/2026-07-02-part4-findings.md`，本地）；可按需在 dotenv 里调（`OPENPE_*_MAX_MESSAGES/_MAX_CHARS`）。

### Q3. 为什么「第一次没带历史、第二次带了」？两次增强的 prompt 为什么不同？

> 2026-07 更新：本题描述的是**回退启发式路径**的现象。Linux 精确识别（Q1 第 1 级）下该场景已消失——resume 昨天的会话后第一条 `pe` 就能带上历史（识别成功不受时效窗限制）。以下机制仍适用于非 Linux / 探测失败的回退路径。

先纠正两个说法：

- **第一次不是「失败」**：它成功产出了一条合法的增强 prompt，只是**没有历史**（因此比较通用/欠具体）。失败是指 provider 报错或空输出，那会是另一种提示。
- **openPE 没有「激活」任何上下文**：openPE 对会话库是**只读**的，从不写入、也不会去「激活」窗口。

真正发生的是——**会话的「最近活跃时间戳」在两次调用之间越过了时效窗口**：

1. 早上你**刚恢复**一个昨晚就在用的长会话。第一条 `pe` 触发时，这条 prompt 还没被提交，会话的最近活跃时间戳仍停在**昨晚**（超出时效窗口）→ 定位为 **stale** → 不带历史 → 通用增强（就是你看到的那条）。
2. 之后你与该会话有了新的交互（提交/继续对话），**Devin 把该会话的最近活跃时间戳更新到「刚刚」**。
3. 第二条 `pe` 触发时，**同一个会话**已在窗口内（fresh）→ 命中历史 → 取它最近的约 12 条（含 Phase 2/3 讨论）→ 上下文丰富的增强。

所以你的直觉「第一条让第二条带上了上下文」——**结果上对，机制上要修正**：让会话变「新鲜」的是**你自己的活跃**被 Devin 记录，不是 openPE；而且第一条并没有「失败」。两次 prompt 不同，正是因为第二次真的带了那段 Phase 2/3 历史、第一次没带。

### Q4. 我想让「继续 xxx」这类延续 prompt 总能带上历史，怎么办？

- **Linux 下升级到含精确识别的版本即可**（2026-07 起）：识别成功时延续 prompt 总能命中本会话历史，无需调窗口。以下手段仅在回退路径下才需要：
- 同日常复用：把窗口调大，写进 hook dotenv（如 `~/.config/openpe/.env`）：`OPENPE_DEVIN_HISTORY_RECENCY=12h`。
- 或：先在会话里有一次新活跃，再敲 `pe`（现象示例里第二次拿到满血版就是这么来的）。
- **取舍**：窗口调得越大，越可能把「其实已经废弃的同目录旧会话」的陈旧上下文也带进来。默认 **6h** 是「延续性 vs 防陈旧泄漏」的折中（2026-07 由 2h 上调：2h 下"隔夜/隔半天恢复"的第一条延续 prompt 总是落空，正是现象示例那种情况）。

> 延伸：延续/指代型 prompt（「继续 Phase 3」「把它也改了」）在**没有历史**时只能被增强成通用版本——这与本项目对「指代解析依赖历史」的评测结论一致（见 `docs/development/2026-07-01-part3-refbind-ab-findings.md`，本地）。

### Q5. 会话被压缩（compaction/续聊摘要）之后，第一条 `pe` 有没有历史？

**有——压缩摘要会作为历史带入**（2026-07-03 起），披露会写「已带入 N 条会话历史（含前文压缩摘要）」。

机制：Devin 压缩会话时，会把主链上的前文替换成一条 **system 角色**的摘要节点（你看到的"You are continuing work from a previous conversation thread…"就是它，携带上一段对话的完整总结）。openPE 的历史采集本来只保留 user/assistant 轮次、过滤 system 噪声——2026-07-03 的事故正是这个过滤器把摘要也丢了：压缩后紧接着的 `pe` 报「会话历史为空」（其实库里有近万个节点），增强退化成零上下文通用版（"汇报一下现在的状态"被膨胀成一张通用仓库体检清单）。修复后，带 `summarized_from` 标记的摘要节点被识别并**映射为 assistant 轮次**带入。

两个边界，防止误解：

- 摘要**占用同一份 12 条 / 12000 字预算**（超长摘要会被截断，新对话轮次积累多了之后摘要会先被挤出——此时披露也不再声称"含摘要"）。
- 若压缩后你已经聊了很多轮，摘要被挤出属正常裁剪，不是回归。

### Q6. 「会话历史为空」还会在什么情况下出现？

修复压缩场景后，这个披露只剩真正的空会话：定位到的会话确实还没有任何可复用的 user/assistant 轮次（例如全新会话的第一条消息就是 `pe`，或此前轮次都是 system/tool 噪声、被取消的空回复）。看到它时增强仍会成功产出，只是不含前文——通用化是预期行为，不是失败。

---

## 二、交付与「Prompt blocked」

### Q7. 为什么显示「Prompt blocked / openPE 已拦截原始消息」？剪贴板里是原文还是增强结果？

这是**非注入（review）模式**的正常成功交付（默认 `OPENPE_HOOK_INJECT=false`，各客户端可用 `OPENPE_<CLIENT>_INJECT` 覆盖）：

- openPE **拦下你那行原始 `pe：…`**（不让 agent 去执行这段字面量），然后把**增强后的 prompt** 放进剪贴板；
- 「拦截原始消息」= **挡下原始输入**，**不是**「只复制了原文」。剪贴板里是**增强结果**，粘贴（Ctrl/Cmd+V）、按需编辑、再发送即可。

### Q8. 提示发了 OSC52，但粘贴出来还是旧内容？

OSC52 需要**终端支持**才能真正写入系统剪贴板。若你的终端/IDE 子进程不认 OSC52（如无控制 TTY 的 IDE 子进程、部分远程终端），剪贴板不会更新，这时粘贴到的是**旧内容**。判断与兜底：

- 粘贴后若发现不是本次增强结果 → 用 `openpe <client> hook last --prompt` 打印，或直接打开缓存文件 `last-prompt.txt`（路径见 stderr / 「调用方式」）。
- 若 stderr 明确出现「剪贴板未更新，请勿直接粘贴旧内容」→ **不要按 Ctrl+V**，走上面的兜底。

### Q9. 我想让它直接注入而不是「拦截 + 剪贴板」，可以吗？

可以（在支持注入的宿主：Devin / Codex / Claude）。设 `OPENPE_HOOK_INJECT=true`（全局）或 `OPENPE_<CLIENT>_INJECT=true`（单客户端）。默认关闭是 openPE 的「绝不自动应用、让你先审阅」哲学；Windsurf 无法接收 hook 注入，该开关在其上是有记录的空操作。

### Q10. 拦截后我立刻重发了同一条 `pe`（比如粘贴失败想重试），会发生什么？

**5 秒去重窗口内（同一会话）：重放上次的拦截，不重新增强**（2026-07-03 起）。你看到的消息与首次拦截**完全一致**（历史披露、告警原样复现），缓存里的增强结果会**再次复制到剪贴板**——重试因此真正有用（粘贴失败的场景正是靠这次重投递兜底）。窗口外重发则正常重新增强。

背景（2026-07-03 两起事故的完整链条）：

1. 去重机制本来是给「Devin 一次提交触发多个 openPE hook」设计的（你把 openPE 同时装在 claude/windsurf 格式时，Devin 会为一次提交把它们**全部跑一遍**，先拦截也不短路）。它无法区分「同一次提交的兄弟 hook」和「你几秒内重发同一文本」——旧版对两者一律 `continue` 放行，导致用户重发时**原始 `pe：…` 原文被直接放行进了模型**（事故一，上午）。
2. 第一次修复改为「重放拦截 + 提示『检测到重复提交同一消息，已复用…』」——但 Devin 展示的是**最后一个 hook** 的拦截理由，于是多 hook 并装时**每一次首次提交**都被这条提示顶掉了正常披露，看起来像"第一次发就被说成重复提交"（事故二，下午，`/rename-session 后端开发者1号` 后首条 kickoff 即触发）。
3. 现行为：重放不再自我标注，而是**原样复现获胜 hook 的披露 + 重新投递缓存增强**——兄弟 hook 场景下与首次消息浑然一体，重发场景下拿到的信息与首次一致。审计增强产物用 `openpe devin hook last --prompt`。

另两条边界：注入模式的获胜者之后，重复触发仍是**静默跳过**（重放注入会把同一条消息注入两次）；去重按**会话隔离**（Linux），同一段模板 kickoff 粘进两个并行会话（多角色工作流）不会互相去重，各自独立增强。

---

## 三、增强改写质量

### Q11. 增强结果里出现了上下文里没有的数字，或者口吻变成了"助手在问我"？

这两类劣化有真实案例（2026-07-02）：用户对助手报告回一句「验证通过，继续推进」，增强结果把报告里"需要你决策/我可以起草/请明确你希望哪项"等**助手对用户说的话**原样吸收（人称完全反转），还给上下文中没给出数字的条目**编造精确数值**（且每次编的不一样）。

- **缓解情况**：内置提示词 v7g（2026-07）加入两条护栏并在末尾复检——增强结果必须是「用户对 agent 的祈使指令」（助手的反问/自称要转写或删除）；数字/组成只能逐字取自上下文中同一条目，总数不得分解成编造的分项。探针矩阵（17 样本 × 6 模型端点）内口吻反转清零、编造数字降为 ~12% 残余；**属候选缓解，系统性验证进行中**。
- **残余怎么防**：剩余臆造集中在「给只提了名字的条目补一个括号数字」。默认 review 交付流就是兜底：**粘贴前扫一眼输出里的数字**，不认识的数字就删掉或改回模糊表述。若你用注入模式（`OPENPE_*_INJECT=true`，跳过人工过目），对含数字汇报类的回应建议把关键数值直接写进你的原始 prompt。

### Q12. 我问的是"你觉得该做什么？"，增强结果怎么变成了一张要执行的任务单？

这是 2026-07-02 的**事故 #3**（v7g 上线当日暴露）：征询式问题「你觉得现在应该做什么？继续基准测试还是做其他方面？」被增强成完整执行指令单，并把上下文里"本地 13 个提交**尚未 push**"这个**状态陈述**翻转成了「push 到远程」的**指令**——用户从未做过这个决策，而下游 agent 按指令真的推送了。

- **缓解情况**：v7h（2026-07）限定"回应→指令"转换**仅适用于用户输入本身表达了决策**（认可/批准/继续/明确选择）；输入是问题时增强结果必须仍是**咨询请求**（评估+建议+理由，可枚举上下文中的候选方向），并明确「上下文中的状态是事实不是授权」，FINAL CHECK 增加对 push/部署/删除/发布等未决策动作的复检。探针级验证：v7h 在 sonnet/opus/gpt-5.5 上 7/7 干净（含模型主动写出"不要擅自执行 push，这里只需要评估和建议"），对照 v7g 为 0/3——**候选缓解**，"合法决策不被弱化"等对照场景由系统性验证覆盖后才下最终结论。
- **教训与兜底**：增强结果里出现**你没决定过的动作**（尤其 push/部署/删除这类不可逆操作）就删掉再发；注入模式用户请特别注意这一条。
